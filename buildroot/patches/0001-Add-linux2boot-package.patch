    diff --git a/../buildroot-2020.02/package/Config.in b/package/Config.in
index edf7687..bd377c7 100644
--- a/../buildroot-2020.02/package/Config.in
+++ b/package/Config.in
@@ -2308,6 +2308,7 @@ menu "System tools"
 	source "package/kmod/Config.in"
 	source "package/kvmtool/Config.in"
 	source "package/libostree/Config.in"
+	source "package/linux2boot/Config.in"
 	source "package/lxc/Config.in"
 	source "package/mender/Config.in"
 	source "package/mender-grubenv/Config.in"
diff --git a/package/linux2boot/Config.in b/package/linux2boot/Config.in
new file mode 100644
index 0000000..c369907
--- /dev/null
+++ b/package/linux2boot/Config.in
@@ -0,0 +1,4 @@
+config BR2_PACKAGE_LINUX2BOOT
+	bool "linux2boot"
+	help
+	  linux2boot is a tiny kexec-based bootloader
diff --git a/package/linux2boot/fs-overlay/etc/inittab b/package/linux2boot/fs-overlay/etc/inittab
new file mode 100644
index 0000000..9d4d88a
--- /dev/null
+++ b/package/linux2boot/fs-overlay/etc/inittab
@@ -0,0 +1,42 @@
+# /etc/inittab
+#
+# Copyright (C) 2001 Erik Andersen <andersen@codepoet.org>
+#
+# Note: BusyBox init doesn't support runlevels.  The runlevels field is
+# completely ignored by BusyBox init. If you want runlevels, use
+# sysvinit.
+#
+# Format for each entry: <id>:<runlevels>:<action>:<process>
+#
+# id        == tty to run on, or empty for /dev/console
+# runlevels == ignored
+# action    == one of sysinit, respawn, askfirst, wait, and once
+# process   == program to run
+
+# Startup the system
+::sysinit:/bin/mount -t proc proc /proc
+::sysinit:/bin/mount -o remount,rw /
+::sysinit:/bin/mkdir -p /dev/pts /dev/shm
+::sysinit:/bin/mount -a
+::sysinit:/sbin/swapon -a
+null::sysinit:/bin/ln -sf /proc/self/fd /dev/fd
+null::sysinit:/bin/ln -sf /proc/self/fd/0 /dev/stdin
+null::sysinit:/bin/ln -sf /proc/self/fd/1 /dev/stdout
+null::sysinit:/bin/ln -sf /proc/self/fd/2 /dev/stderr
+::sysinit:/bin/hostname -F /etc/hostname
+# now run any rc scripts
+::sysinit:/etc/init.d/rcS
+
+# Put a getty on the serial port
+console::respawn:/sbin/getty -L  console 0 vt100 # GENERIC_SERIAL
+
+tty0::respawn:/sbin/getty -n -l /usr/sbin/linux2boot 0 tty0
+
+# Stuff to do for the 3-finger salute
+#::ctrlaltdel:/sbin/reboot
+
+# Stuff to do before rebooting
+::shutdown:/etc/init.d/rcK
+::shutdown:/sbin/swapoff -a
+::shutdown:/bin/umount -a -r
+
diff --git a/package/linux2boot/linux2boot.mk b/package/linux2boot/linux2boot.mk
new file mode 100644
index 0000000..bb0067c
--- /dev/null
+++ b/package/linux2boot/linux2boot.mk
@@ -0,0 +1,16 @@
+################################################################################
+#
+# linux2boot
+#
+################################################################################
+
+LINUX2BOOT_VERSION = v0.1
+LINUX2BOOT_SITE = $(call github,TobleMiner,linux2boot,$(LINUX2BOOT_VERSION))
+LINUX2BOOT_LICENSE = MIT
+LINUX2BOOT_LICENSE_FILES = LICENSE
+
+define LINUX2BOOT_INSTALL_TARGET_CMDS
+	$(INSTALL) -D -m 0755 $(@D)/linux2boot $(TARGET_DIR)/usr/sbin/
+endef
+
+$(eval $(generic-package))
